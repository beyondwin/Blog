---
import { publicNav } from '../lib/siteChrome';
---
<footer class="site-footer">
  <span>© beyondwin</span>
  <nav aria-label="하단 탐색">
    {publicNav.map((item) => <a href={item.href}>{item.label}</a>)}
  </nav>
</footer>
